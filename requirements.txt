# Phase 1 dataset inspection + later visualisation support only.
# The core Big Data pipeline is Scala + Spark; Python is used exclusively
# for one-off EDA and for plotting in Phase 19.
pandas>=2.0.0
numpy>=1.24.0

# Added in Phase 19 (visualisation)
matplotlib>=3.7.0
seaborn>=0.12.0
